
																						
																						
																	
Leaf::Action::ActionDie	死亡动作：让 AI 执行死亡动画、状态切换（如 “生命值清零→播放死亡动画→标记为死亡状态”）。	这是一个用于 ** 控制实体执行 “死亡动作”** 的行为树节点，名为ActionDie，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为Delegate（委托）
当前值：$niy
作用：标识节点的核心功能为 “死亡动作”，用于定义或关联实体执行死亡逻辑的核心指令（如角色死亡动画、状态切换、触发死亡后事件等）。"																
		Leaf::Action::ActionDroneMove	无人机移动：控制无人机类型 AI 的基础移动（如直线移动、跟随目标）。	这是一个用于无人机移动控制的行为树节点，名为ActionDroneMove，属于行为树中的动作节点（负责执行具体行为）。		"1. Command（命令类型）
类型：Serializable（可序列化，便于数据存储与传输）
该参数定义节点的核心功能为 “无人机移动动作”，是节点的身份标识。
2. Move Type（移动类型）
类型：Enum（枚举类型，用于选择预设的移动模式，如直线移动、绕点移动等，当前值为@None表示未指定）
枚举类型让开发者可以快速选择标准化的移动逻辑，无需手动编写复杂流程。
3. Target（目标对象）
类型：Object（对象类型，用于指定无人机移动的目标实体，如某个游戏对象、地标点，当前为NULL表示未设置）
若需无人机向特定实体移动（如追逐目标、前往建筑），可在此绑定对象。
4. Movement Target（移动目标坐标）
类型：Vector（三维向量，用于指定具体的空间坐标(X, Y, Z)，当前值为(0, 0, 0)）
当需要无人机移动到地图上的精确坐标（如资源点、救援位置）时，通过该参数设置。
5. Tolerance Radius（容差半径）
类型：Float（浮点型，用于定义无人机到达目标区域的 “误差范围”，当前为0）
例如设置为5时，无人机只要进入目标点周围 5 米范围内，就判定为 “到达”，避免因精度问题持续空转。
6. Desired Distance From Target（与目标的期望距离）
类型：Float（浮点型，用于指定无人机与目标保持的距离，当前为0）
若需无人机 “跟随目标但保持 3 米距离” 或 “在目标 10 米外盘旋”，可通过该参数实现。
7. Strafing Target（侧移目标）
类型：CName（命名类型，用于指定侧移的参考目标，当前为None）
侧移常用于无人机调整姿态（如绕目标横向移动、规避障碍物时的侧向偏移）。
8. Stop When Destination Reached（到达目的地时停止）
类型：Bool（布尔型，用于选择是否在到达目标后停止移动，当前为false）
若为true，无人机到达目标后会停止移动；若为false，可能继续执行后续逻辑（如到达后执行攻击、采集动作）。
9. Rotate Entity（实体旋转）
类型：Bool（布尔型，用于选择是否在移动过程中同时旋转无人机朝向，当前为false）
若需无人机移动时始终朝向目标（如摄像头对准目标），可将该参数设为true。
节点作用与应用场景
该节点为无人机 AI 的移动行为提供了高度可配置的模块化解决方案，适用于游戏、仿真、机器人控制等领域。例如：
在无人机模拟游戏中，用于控制无人机前往任务点、追逐敌人；
在工业巡检仿真中，用于控制无人机按坐标遍历巡检点；
在 AI 研究中，用于测试不同移动逻辑对无人机行为的影响。"																
		Leaf::Action::ActionDroneMoveSpline	无人机样条线移动：控制无人机沿预设样条线（Spline）移动（如固定航线巡逻）。	这是一个用于无人机沿路径移动控制的行为树节点，名为ActionDroneMoveSpline，属于动作节点（Action Node），专注于让无人机沿预设的样条曲线（Spline）执行移动行为。		"这是一个用于无人机沿路径移动控制的行为树节点，名为ActionDroneMoveSpline，属于动作节点（Action Node），专注于让无人机沿预设的样条曲线（Spline）执行移动行为。以下是其核心参数的详细说明：
1. Debug Log Comment（调试日志注释）
用于在调试时添加自定义日志信息，方便追踪节点执行过程，当前未设置具体内容。
2. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：定义节点的核心指令标识，当前值为None（未指定具体命令），可能用于区分不同的移动模式或触发特定逻辑（如 “开始沿路径移动”“暂停路径” 等）。
3. Spline（样条曲线）
类型：NodeRef（节点引用），参数化类型为CustomValue（自定义值）
核心作用：指定无人机需要沿其移动的样条曲线路径。样条曲线是一种平滑的连续曲线，常用于定义复杂路径（如弧形、多段折线过渡等）。
此处Value未设置，实际使用时需关联到预设的样条曲线节点（如场景中绘制的路径数据），无人机将沿该曲线的轨迹移动。"																
		Leaf::Action::ActionDynamicMove	动态移动：AI 根据实时环境动态调整移动路径（如避开突发障碍物、跟随移动的目标）。	这是一个用于动态移动控制的行为树节点，名为ActionDynamicMove，属于动作节点，用于让实体（如角色、无人机等）执行动态移动行为。		"1. Command（命令）
类型：Serializable（可序列化，便于数据存储与传输）
用于标识该节点的核心功能为 “动态移动动作”，是节点的身份标识，当前值为NULL。
2. Move Type（移动类型）
类型：Enum（枚举类型，用于选择预设的移动模式，如追击、规避、巡逻等，当前值为@None表示未指定）
枚举类型可快速选择标准化的移动逻辑，适配不同场景的移动需求。
3. Target（目标对象）
类型：Object（对象类型，用于指定移动的目标实体，如敌人、资源点等，当前为NULL）
若需实体向特定对象移动（如角色追逐敌人、机器人前往工作台），可在此绑定对象。
4. Target Position（目标位置）
类型：Vector（三维向量，用于指定具体的空间坐标{0, 0, 0}）
当需要实体移动到地图上的精确坐标（如任务点、出生点）时，通过该参数设置。
5. Tolerance Radius（容差半径）
类型：Float（浮点型，用于定义实体到达目标区域的 “误差范围”，当前为0）
例如设置为3时，实体只要进入目标点周围 3 单位范围内，就判定为 “到达”，避免因精度问题持续无效移动。
6. Desired Distance From Target（与目标的期望距离）
类型：Float（浮点型，用于指定实体与目标保持的距离，当前为0）
若需实体 “与目标保持 5 单位距离跟随” 或 “在目标 10 单位外待命”，可通过该参数实现。
7. Strafing Target（侧移目标）
类型：Object（对象类型，当前为NULL）
用于实体侧移时的参考目标，可实现绕目标横向移动、规避障碍物时的侧向偏移等逻辑。
8. Stop When Destination Reached（到达目的地时停止）
类型：Bool（布尔型，用于选择是否在到达目标后停止移动，当前为false）
若为true，实体到达目标后会停止移动；若为false，可能继续执行后续逻辑（如到达后攻击、交互）。
9. Rotate Entity（实体旋转）
类型：Bool（布尔型，用于选择是否在移动过程中同时旋转实体朝向，当前为false）
若需实体移动时始终朝向目标（如角色面朝敌人、摄像头对准目标），可将该参数设为true。
10. Ignore Navigation（忽略导航）
类型：Bool（布尔型，用于选择是否忽略场景中的导航网格 / 路径规划，当前为false）
若为true，实体将直接向目标移动，不考虑地形、障碍物等限制；若为false，则会遵循导航规则，绕开障碍物到达目标。"																
		Leaf::Action::ActionMove	基础移动：AI 的通用移动（如向指定位置移动、跟随指定对象）。	这是一个用于通用移动控制的行为树节点，名为ActionMove，属于动作节点（Action Node），用于让实体（如角色、机器人、无人机等）执行移动行为。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：定义节点的核心指令标识，当前值为None（未指定具体命令），可用于区分不同的移动模式或触发特定逻辑（如 “开始移动”“紧急移动” 等）。
2. Movement Target（移动目标坐标）
类型：Vector（三维向量），参数化类型为CustomValue（自定义值）
核心作用：指定实体需要移动到的空间坐标(X, Y, Z)，当前值为(0, 0, 0)。实际使用时需设置具体坐标，作为实体移动的终点。
3. Look At Target（朝向目标）
用于指定实体移动过程中需要朝向的目标对象（如敌人、地标），当前为NULL（未设置）。若配置该参数，实体移动时会同时旋转朝向该目标。
4. Movement Type（移动类型）
类型：Enum（枚举类型），当前值为@None（未指定）
作用：选择预设的移动模式（如直线移动、曲线移动、跟随移动等），通过枚举快速复用标准化移动逻辑。
5. Tolerance（容差）
类型：Float（浮点型），当前为0
作用：定义实体到达目标区域的 “误差范围”。例如设置为2时，实体进入目标点周围 2 单位范围内即判定为 “到达”，避免因精度问题持续无效移动。
6. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为False
作用：选择是否忽略场景中的导航网格 / 路径规划。若为True，实体将直接向目标移动，无视地形、障碍物；若为False，则遵循导航规则绕开障碍。
7. Rotate Entity（实体旋转）
类型：Bool（布尔型），当前为False
作用：选择是否在移动过程中旋转实体朝向（如朝向目标、保持特定方向）。
8. Use Start / Use Stop（使用起点 / 终点）
类型：Bool（布尔型），当前均为False
作用：启用后可自定义移动的 “起点” 或 “终点” 逻辑，适配更复杂的移动场景（如从指定点出发、到达后执行停止动作）。
9. Destination Tangent / Start Tangent（终点 / 起点切线）
类型：Vector（三维向量），当前均为(0, 0, 0)
作用：定义移动起点或终点的 “切线方向”，用于控制移动的平滑过渡（如曲线移动的起始 / 结束朝向）。
10. Spot Reservation（点位预留）
类型：Bool（布尔型），当前为False
作用：启用后可预留目标点位，避免多个实体同时抢占同一位置。
11. Ignore Restricted Area（忽略限制区域）
类型：Bool（布尔型），当前为False
作用：选择是否忽略场景中标记的 “限制区域”（如禁区、危险区），若为True则可强制进入这些区域。
节点功能与应用场景"																
		Leaf::Action::ActionMoveOnSpline	样条线移动：AI 沿预设样条线移动（如固定路线巡逻、沿赛道行驶）。	这是一个用于沿样条曲线移动控制的行为树节点，名为ActionMoveOnSpline，属于动作节点（Action Node），专注于让实体沿预设的样条曲线（Spline）执行移动行为。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：定义节点的核心指令标识，当前值为None（未指定具体命令），可用于区分不同的移动模式或触发特定逻辑（如 “开始沿曲线移动”“暂停曲线移动” 等）。
2. Spline（样条曲线）
类型：NodeRef（节点引用），参数化类型为CustomValue（自定义值）
核心作用：指定实体需要沿其移动的样条曲线路径。样条曲线是一种平滑的连续曲线，可用于定义复杂轨迹（如弧形、多段过渡曲线等）。实际使用时需关联到场景中预设的样条曲线节点，当前Value未设置。
3. Strafing Target（侧移目标）
类型：Object（对象类型），当前为NULL
作用：实体侧移时的参考目标，用于实现绕目标横向移动、规避障碍物时的侧向偏移等逻辑。
4. Movement Type（移动类型）
类型：Enum（枚举类型），当前值为@None（未指定）
作用：选择预设的移动模式（如匀速移动、变速移动、跟随曲线曲率移动等），通过枚举快速复用标准化移动逻辑。
5. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景中的导航网格 / 路径规划。若为true，实体将直接沿曲线移动，无视地形、障碍物；若为false，则遵循导航规则（若曲线与导航冲突，可能出现异常）。
6. Snap To Terrain（贴地）
类型：Bool（布尔型），当前为false
作用：启用后实体将沿曲线贴附地形表面移动，避免悬空或穿模，适用于地面单位（如车辆、机器人）。
7. Rotate Entity（实体旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在移动过程中旋转实体朝向（如沿曲线切线方向朝向、朝向侧移目标等）。
8. Start From Closest Point（从最近点开始）
类型：Bool（布尔型），当前为false
作用：启用后实体将从曲线上距离自身最近的点开始移动，而非从曲线起点开始，适用于动态触发的曲线移动场景。
9. Use Start / Use Stop（使用起点 / 终点）
类型：Bool（布尔型），当前均为false
作用：启用后可自定义曲线移动的 “起点段” 或 “终点段” 逻辑，如在起点 / 终点处执行加速、减速等动作。
10. Reverse（反向）
类型：Bool（布尔型），当前为false
作用：启用后实体将沿曲线反向移动（从终点向起点移动）。
11. Is Back And Forth（来回移动）
类型：Bool（布尔型），当前为false
作用：启用后实体将在曲线起点和终点之间来回移动，形成往复轨迹。
12. Is Infinite（无限循环）
类型：Bool（布尔型），当前为false
作用：启用后实体将沿曲线无限循环移动，适用于巡逻、展示等需要持续移动的场景。
13. Number Of Loops（循环次数）
类型：Float（浮点型），当前为0
作用：设置实体沿曲线循环移动的次数，需配合Is Infinite为false使用。
14. Use Off Mesh Link Reservation（使用离网链接预留）
类型：Bool（布尔型），当前为false
作用：启用后预留曲线移动涉及的离网链接（导航网格外的路径），避免多实体冲突，适用于复杂导航场景。"																
		Leaf::Action::ActionMoveToSmartObject	移动到智能对象：AI 移动到预设的 “智能对象”（如可交互的门、宝箱、机关）旁，为后续交互做准备。	这是一个用于向智能对象移动并交互的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “向智能对象移动”，当前值为None（未指定具体命令）。
2. Smart Object Id（智能对象 ID）
用于指定目标智能对象的唯一标识，智能对象是指场景中可交互的复杂实体（如带交互逻辑的机器、建筑、道具等），当前为0（未设置）。
3. Look At Target（朝向目标）
定义实体移动过程中需要朝向的目标对象，当前为NULL（未设置），若配置可让实体移动时保持朝向该目标。
4. Movement Type（移动类型）
类型：Enum（枚举类型），当前值为@None（未指定）
作用：选择预设的移动模式（如直线移动、曲线移动等），快速复用标准化移动逻辑。
5. Tolerance（容差）
类型：Float（浮点型），当前为0
作用：定义实体到达智能对象区域的误差范围，例如设置为2时，进入目标周围 2 单位范围即判定为 “到达”。
6. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格，false时遵循导航规则绕开障碍，true时直接向目标移动。
7. Rotate Entity（实体旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在移动过程中旋转实体朝向（如朝向智能对象、保持特定方向）。
8. Use Start / Use Stop（使用起点 / 终点）
类型：Bool（布尔型），当前均为false
作用：启用后可自定义移动的起点或终点逻辑（如起点加速、终点减速）。
9. Forced Entry Animation（强制进入动画）
用于指定实体到达智能对象时触发的进入动画，当前为None（未设置），可实现与智能对象交互的动画衔接（如角色坐椅子的动画）。"																
		Leaf::Action::ActionMoveToWorkspot	移动到工作点：AI 移动到指定工作点（如椅子、工具台、站岗位置）。	这是一个用于向工作点移动并作业的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “向工作点位移动”，当前值为None（未指定具体命令）。
2. Workspot Setup（工作点位配置）
用于指定目标工作点位的配置信息，工作点位是指场景中预设的作业区域（如工作台、操作工位等），当前为NULL（未设置）。
3. Look At Target（朝向目标）
定义实体移动过程中需要朝向的目标对象，当前为NULL（未设置），若配置可让实体移动时保持朝向该目标。
4. Movement Type（移动类型）
类型：Enum（枚举类型），当前值为@None（未指定）
作用：选择预设的移动模式（如直线移动、曲线移动等），快速复用标准化移动逻辑。
5. Tolerance（容差）
类型：Float（浮点型），当前为0
作用：定义实体到达工作点位区域的误差范围，例如设置为2时，进入目标周围 2 单位范围即判定为 “到达”。
6. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格，false时遵循导航规则绕开障碍，true时直接向目标移动。
7. Rotate Entity（实体旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在移动过程中旋转实体朝向（如朝向工作点位、保持特定方向）。
8. Use Start（使用起点）
类型：Bool（布尔型），当前为false
作用：启用后可自定义移动的起点逻辑（如起点加速）。
9. Spot Reservation（点位预留）
类型：Bool（布尔型），当前为false
作用：启用后预留工作点位，避免多个实体同时抢占同一作业区域。
10. Start Tangent（起点切线）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：定义移动起点的切线方向，用于控制移动的平滑过渡（如曲线移动的起始朝向）。
11. Fast Forward After Teleport（传送后快进）
类型：Bool（布尔型），当前为false
作用：若实体通过传送到达工作点位附近，启用后可快速进入作业状态。
12. Ignore Exploration（忽略探索）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景中的探索限制，确保实体能顺利到达工作点位。"																
		Leaf::Action::ActionMoveWithPolicy	带策略移动：按预设策略移动（如 “潜行移动”“快速冲刺”“节能移动”），适配不同场景需求。	这是一个用于带策略的移动控制的行为树节点，名为ActionMoveWithPolicy，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “带策略的移动”，当前值为None（未指定具体命令）。该参数用于定义移动策略的类型（如 “优先快速移动”“优先避障移动” 等）。
2. Stop When Destination Reached（到达目的地时停止）
类型：Bool（布尔型）
作用：选择是否在实体到达目标后停止移动。若为true，到达后停止；若为false，可能继续执行后续与移动策略相关的逻辑。"																
																	
		Leaf::Action::ActionRotateBase::ActionRotateToObject	转向对象：AI 朝向指定对象（如敌人、友军、交互物）。	这是一个用于实体旋转朝向目标对象的行为树节点，名为ActionRotateToObject		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “旋转朝向对象”，当前值为None（未指定具体命令）。
2. Target（目标对象）
类型：Object（对象类型），参数化类型为CustomValue（自定义值）
作用：指定实体需要朝向的目标对象（如敌人、道具、地标等），当前为NULL（未设置）。
3. Angle Offset（角度偏移）
类型：Float（浮点型），当前为0
作用：定义实体朝向目标时的角度偏移量。例如设置为30，实体将朝向目标对象的 30 度方向，而非正对着目标。
4. Angle Tolerance（角度容差）
类型：Float（浮点型），当前为0
作用：定义实体朝向目标的角度误差范围。例如设置为5，只要实体与目标的角度差在 5 度以内，就判定为 “朝向完成”。
5. Speed（旋转速度）
类型：Float（浮点型）
作用：设置实体旋转朝向目标的速度，数值越大旋转越快。
6. Complete When Rotated（旋转完成时结束）
类型：Bool（布尔型），当前为false
作用：选择是否在实体完成朝向目标后结束该节点的执行。若为true，旋转完成后节点执行完毕；若为false，可能持续检测朝向状态。"																
		Leaf::Action::ActionRotateBase::ActionRotateToObjectHeading	朝向对象朝向：AI 转向与指定对象相同的朝向（如跟随队友的前进方向）。	这是一个用于控制实体朝向目标对象并控制朝向方向（Heading）** 的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “朝向目标对象并控制朝向方向”，当前值为None（未指定具体命令）。
2. Target（目标对象）
类型：Object（对象类型），参数化类型为CustomValue（自定义值）
作用：指定实体需要朝向的目标对象（如敌人、交互道具、地标等），当前为NULL（未设置）。
3. Angle Offset（角度偏移）
类型：Float（浮点型），当前为0
作用：定义实体朝向目标时的角度偏移量。例如设置为45，实体将朝向目标对象的 45 度方向，而非正对着目标。
4. Angle Tolerance（角度容差）
类型：Float（浮点型），当前为0
作用：定义实体朝向目标的角度误差范围。例如设置为3，只要实体与目标的角度差在 3 度以内，就判定为 “朝向完成”。
5. Speed（旋转速度）
类型：Float（浮点型）
作用：设置实体旋转朝向目标的速度，数值越大旋转越快。"																
		Leaf::Action::ActionRotateBase::ActionRotateToPosition	朝向位置：AI 朝向指定世界坐标（如预设的警戒方向、任务目标点）。	这是一个用于实体旋转朝向目标坐标位置的行为树节点，名为ActionRotateToPosition，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “旋转朝向坐标位置”，当前值为None（未指定具体命令）。
2. Target（目标坐标）
类型：Vector（三维向量），参数化类型为CustomValue（自定义值）
作用：指定实体需要朝向的空间坐标{X, Y, Z}，当前值为{0, 0, 0}。实际使用时需设置具体坐标，作为实体旋转的朝向目标。
3. Angle Offset（角度偏移）
类型：Float（浮点型）
作用：定义实体朝向目标坐标时的角度偏移量。例如设置为30，实体将朝向目标坐标的 30 度方向，而非正对着坐标点。
4. Angle Tolerance（角度容差）
类型：Float（浮点型）
作用：定义实体朝向目标坐标的角度误差范围。例如设置为5，只要实体与目标坐标的角度差在 5 度以内，就判定为 “朝向完成”。
5. Speed（旋转速度）
类型：Float（浮点型）
作用：设置实体旋转朝向目标坐标的速度，数值越大旋转越快。"																
		Leaf::Action::ActionRotateByAngle	按角度旋转：AI 按指定角度旋转（如顺时针旋转 90 度、逆时针旋转 45 度）。	这是一个用于实体按指定角度旋转的行为树节点，名为ActionRotateByAngle		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “按角度旋转”，当前值为None（未指定具体命令）。
2. Angle（旋转角度）
类型：Float（浮点型），当前为0
作用：指定实体需要旋转的角度值，可正可负（正数通常表示顺时针旋转，负数表示逆时针旋转，具体需结合引擎定义）。
3. Angle Tolerance（角度容差）
类型：Float（浮点型），当前为0
作用：定义实体旋转角度的误差范围。例如设置为2，只要实体实际旋转角度与目标角度的差值在 2 度以内，就判定为 “旋转完成”。"																
																	
Leaf::Action::ActionSlide::ActionSlideToLocalPosition	滑行到本地位置：AI 相对于自身当前位置，滑行到指定本地坐标（如 “向前滑行 2 米”）。	这是一个用于实体向自身局部坐标偏移位置滑动移动的行为树节点，名为ActionSlideToLocalPosition		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “向局部坐标滑动移动”，当前值为None（未指定具体命令）。
2. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置实体从当前位置滑动到目标局部位置的总时长，数值越大滑动越平缓。
3. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格，false时遵循导航规则（若滑动路径有障碍会受影响），true时直接向局部目标滑动。
4. Rotate Towards Movement Direction（朝向移动方向旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在滑动过程中旋转实体朝向移动方向，使移动更具方向性（如角色侧滑时朝向滑动方向）。
5. Local Offset（局部偏移）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体相对于自身当前位置的局部坐标偏移量（以实体自身坐标系为参考）。例如设置为{2, 0, 0}，实体将向自身正前方滑动 2 单位距离。"																
		Leaf::Action::ActionSlide::ActionSlideToObject	滑行到对象：AI 滑行到指定对象旁（如滑行到敌人身边进行攻击）。	这是一个用于实体向目标对象滑动移动的行为树节点，名为ActionSlideToObject		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “向目标对象滑动移动”，用于定义或关联该滑动行为的核心指令。
2. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置实体从当前位置滑动到目标对象的总时长，数值越大滑动过程越平缓。
3. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格。false时遵循导航规则（若滑动路径有障碍会受影响），true时直接向目标对象滑动。
4. Rotate Towards Movement Direction（朝向移动方向旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在滑动过程中旋转实体朝向移动方向，使滑动行为更具方向性（如角色滑动时面朝目标方向）。
5. Destination（目标对象）
当前值：NULL
作用：指定实体需要滑动到达的目标对象（如敌人、道具、交互点等），实体将以该对象为目标执行滑动移动。
6. Offset（偏移量）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体相对于目标对象的位置偏移量。例如设置为{2, 0, 0}，实体将滑动到目标对象正前方 2 单位距离的位置。"																
		Leaf::Action::ActionSlide::ToWorldPosition	滑行到世界位置：AI 滑行到指定世界坐标（如预设的滑行终点）。	这是一个用于实体向全局坐标位置滑动移动的行为树节点，名为SlideToWorldPosition		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “向全局坐标滑动移动”，当前值为None（未指定具体命令）。
2. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置实体从当前位置滑动到目标全局位置的总时长，数值越大滑动越平缓。
3. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格，false时遵循导航规则（若滑动路径有障碍会受影响），true时直接向全局目标滑动。
4. Rotate Towards Movement Direction（朝向移动方向旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在滑动过程中旋转实体朝向移动方向，使移动更具方向性（如角色滑动时朝向目标方向）。
5. World Position（全局位置）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体需要滑动到达的全局坐标位置（以场景全局坐标系为参考）。例如设置为{10, 5, 0}，实体将向该场景坐标点滑动。
6. Use Move Planner（使用移动规划器）
类型：Bool（布尔型），当前为false
作用：启用后将通过移动规划器优化滑动路径，适用于复杂导航场景，确保滑动行为更智能、更贴合场景规则。"																
		Leaf::Action::ActionTeleport	传送动作：AI 瞬间移动到指定位置（如 “传送回出生点”“传送至任务目标旁”）。	这是一个用于实体瞬间传送的行为树节点，名为ActionTeleport		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “传送”，当前值为NULL（未指定具体命令细节）。
2. Destination Point（目标点）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体传送的目标坐标位置（以全局坐标系为参考）。例如设置为{5, 0, 10}，实体将瞬间移动到该场景坐标点。
3. Do Nav Test（导航测试）
类型：Bool（布尔型），当前为false
作用：选择是否对目标点进行导航可行性检测。true时会验证目标点是否在导航网格内（避免传送到不可达区域），false时直接传送至目标点（可能穿透地形或障碍物）。
4. Rotation（旋转角度）
类型：数值型，当前为0
作用：设置实体传送后朝向的角度。例如设置为90，实体传送后将沿指定轴向旋转 90 度，调整至目标朝向。"																
		Leaf::Action::ActionUseWorkspot	使用工作点：AI 在当前工作点执行交互行为（如坐在椅子上、使用工具台制作物品）。	这是一个用于实体使用工作点（Workspot） 的行为树节点，名为ActionUseWorkspot		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
作用：标识节点的核心功能为 “使用工作点”，用于定义或关联与工作点交互的核心指令（如触发工作点的特定功能）。
2. Event Data（事件数据）
作用：存储或传递与工作点交互相关的事件数据，例如交互触发的条件、参数或反馈信息，用于在交互过程中传递上下文信息。
3. Play Start Animation Afterwards（之后播放开始动画）
类型：Bool（布尔型）
作用：设置实体在开始使用工作点后，是否自动播放对应的 “开始动画”（如角色走向工作点后的启动动作，如拿起工具、按下按钮等）。true表示播放，false表示不播放。
4. Dependent Workspot Data（依赖的工作点数据）
作用：关联当前工作点所依赖的其他工作点数据，用于处理多个工作点联动的场景（如 A 工作点的使用需依赖 B 工作点的状态，或从 B 工作点获取参数）。
5. Play Exit Automatically（自动播放退出动画）
类型：Bool（布尔型）
作用：设置实体在结束使用工作点时，是否自动播放 “退出动画”（如放下工具、离开工作点的动作）。true表示自动触发退出动画，false需手动触发。
6. Mark Uninterruptable（标记为不可中断）
类型：Bool（布尔型）
作用：设置实体使用工作点的过程是否不可被中断。true时，交互过程中不会被其他行为（如外部指令、事件）打断；false时可被更高优先级的行为中断。"																
		Leaf::Action::AnimationCurvePath	动画曲线路径：AI 沿动画曲线定义的路径移动 / 旋转（如复杂的舞蹈动作、特技移动）。	这是一个用于控制实体沿动画曲线路径运动并关联动画状态的行为树节点，名为AnimationCurvePath		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “沿动画曲线路径运动”，用于定义或关联该行为的核心指令。
2. Node Reference（节点引用）
作用：关联场景中预设的曲线路径节点，实体将沿该曲线执行运动。
3. Controllers Setup Name（控制器配置名称）
当前值：None
作用：指定用于控制该运动行为的动画控制器配置名称，确保运动与动画状态的联动逻辑。
4. Use Start / Use Stop（使用开始 / 结束动画）
类型：Bool（布尔型），当前均为false
作用：设置是否在曲线运动的 “开始” 或 “结束” 阶段播放自定义动画（如启动时的加速动画、结束时的减速动画）。
5. Blend Time（混合时间）
当前值：0
作用：设置动画与运动状态之间的过渡混合时长，使动画切换更平滑。
6. Global In/Out Blend Time（全局入 / 出混合时间）
当前值：0
作用：设置全局范围内动画进入和退出的过渡时长，适配多动画状态的衔接。
7. Turn Character To Match Velocity（角色转向匹配速度方向）
类型：Bool（布尔型），当前为false
作用：启用后，角色在沿曲线运动时会自动转向速度方向，增强运动的自然感（如跑步时面朝前进方向）。
8. Custom Start/ Main / Stop Animation Name（自定义开始 / 主 / 停止动画名称）
当前值：None
作用：分别指定运动 “开始阶段”“主要阶段”“停止阶段” 的自定义动画名称，实现分阶段的动画逻辑。
9. Start / Main / Stop Snap To Terrain（开始 / 主 / 停止阶段贴地）
类型：Bool（布尔型），当前均为false
作用：设置在运动的不同阶段（开始、过程中、停止）是否贴附地形表面，避免实体悬空或穿模。
10. Start / Stop Snap To Terrain Blend Time（开始 / 停止阶段贴地混合时间）
当前值：0
作用：设置贴地逻辑的过渡混合时长，使贴地动作更平滑自然。"																
		Leaf::Action::ControlledByQuestNode	任务节点控制：AI 行为由任务节点直接控制（如 “任务触发时才执行该动作”，与剧情 / 任务绑定）。	这是一个用于由任务（Quest）控制行为逻辑的行为树节点，名为ControlledByQuestNode，属于控制类节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “向目标对象滑动移动”，用于定义或关联该滑动行为的核心指令。
2. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置实体从当前位置滑动到目标对象的总时长，数值越大滑动过程越平缓。
3. Ignore Navigation（忽略导航）
类型：Bool（布尔型），当前为false
作用：选择是否忽略场景导航网格。false时遵循导航规则（若滑动路径有障碍会受影响），true时直接向目标对象滑动。
4. Rotate Towards Movement Direction（朝向移动方向旋转）
类型：Bool（布尔型），当前为false
作用：选择是否在滑动过程中旋转实体朝向移动方向，使滑动行为更具方向性（如角色滑动时面朝目标方向）。
5. Destination（目标对象）
当前值：NULL
作用：指定实体需要滑动到达的目标对象（如敌人、道具、交互点等），实体将以该对象为目标执行滑动移动。
6. Offset（偏移量）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体相对于目标对象的位置偏移量。例如设置为{2, 0, 0}，实体将滑动到目标对象正前方 2 单位距离的位置。"																
		Leaf::Action::Drive	驾驶动作基类：控制 AI 驾驶载具（如汽车、飞机、坐骑）的通用逻辑。	这是一个用于控制实体进入 “怠速 / 待机” 状态的行为树节点，名为DriveIdle，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “怠速控制”，用于定义或关联实体进入怠速状态的核心指令（如停止运动、关闭非必要功能等）。
节点功能与应用场景
DriveIdle专为实体待机行为设计，适用于需要实体进入 “无动作、低消耗” 状态的场景：
游戏 AI：角色无任务时进入待机动画（如站立 idle 动作）、车辆无指令时停止移动进入怠速状态；
机器人仿真：机器人完成任务后进入待机模式，关闭动力系统以外的高消耗模块；
场景叙事：NPC 在无交互时执行待机行为（如巡逻暂停、原地张望），增强场景真实感。"																
		Leaf::Action::Drive::Driveldle	怠速驾驶：载具保持怠速状态（如停车时发动机空转、缓慢移动等待指令）。	这是一个用于控制实体进入 “怠速 / 待机” 状态的行为树节点，名为DriveIdle，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “怠速控制”，用于定义或关联实体进入怠速状态的核心指令（如停止运动、关闭非必要功能等）。
节点功能与应用场景
DriveIdle专为实体待机行为设计，适用于需要实体进入 “无动作、低消耗” 状态的场景：
游戏 AI：角色无任务时进入待机动画（如站立 idle 动作）、车辆无指令时停止移动进入怠速状态；
机器人仿真：机器人完成任务后进入待机模式，关闭动力系统以外的高消耗模块；
场景叙事：NPC 在无交互时执行待机行为（如巡逻暂停、原地张望），增强场景真实感。"																
		Leaf::Action::Drive::DriveJoinTraffic	汇入车流：AI 驾驶载具汇入当前车流，跟随车流速度和路线行驶（如城市道路行驶）。	这是一个用于控制实体加入交通流的行为树节点，名为DriveJoinTraffic，属于动作节点		"Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “加入交通流”，用于定义或关联实体（如车辆、机器人）进入交通系统并遵循交通规则的核心指令。"																
		Leaf::Action::Drive::DriveRacing	竞速驾驶：载具以竞速模式行驶（如高速冲刺、规避赛道障碍物、超越对手）。	这是一个用于控制实体（如车辆）执行竞速类驾驶行为的行为树节点，名为DriveRacing		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “竞速驾驶”，用于定义或关联竞速场景下的驾驶指令（如加速、超车、沿赛道行驶等）。
2. Use Kinematic（使用运动学）
类型：Bool（布尔型），当前为false
作用：选择是否采用运动学物理规则控制实体运动（适用于追求精准轨迹的竞速场景，如赛车沿赛道的物理模拟）。
3. Need Driver（需要驾驶员）
类型：Bool（布尔型），当前为false
作用：设置实体是否需要 “驾驶员” 逻辑（如模拟有人驾驶的 AI 决策，或无人自动驾驶的差异）。
4. Spline（样条曲线）
类型：NodeRef（节点引用）
作用：关联竞速的赛道样条曲线，实体将沿该曲线执行竞速行驶。
5. Secure Time Out（安全超时）
类型：Float（浮点型），当前为0
作用：设置竞速过程中的安全超时阈值，避免实体因异常卡顿而无限等待。
6. Backwards / Reverse（向后 / 反向）
类型：Bool（布尔型），当前均为false
作用：控制实体是否向后行驶或沿曲线反向竞速。
7. Closest（最近点开始）
类型：Bool（布尔型），当前为false
作用：启用后实体从曲线上距离自身最近的点开始竞速，而非从曲线起点。
8. Forced Start Speed（强制启动速度）
类型：Float（浮点型），当前为0
作用：设置实体竞速的初始速度，用于模拟 “起步加速” 等场景。
9. Stop At Path End（路径终点停止）
类型：Bool（布尔型），当前为false
作用：启用后实体到达赛道曲线终点时停止竞速。
10. 距离与橡胶带（Keep Distance / Rubber Banding）系列参数
作用：用于控制实体与其他竞速单位的距离（如保持跟车距离），或通过 “橡胶带” 逻辑模拟竞速中的距离约束（如防止落后过多时瞬间追上）。"																
		Leaf::Action::Drive::DriveSummoning	召唤载具驾驶：AI 召唤载具后立即驾驶（如 “召唤坐骑→自动骑上并移动”）。	这是一个用于控制实体（如车辆、机器人）执行 “召唤式驾驶” 行为的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “召唤式驾驶”，用于定义或关联实体响应召唤指令的驾驶逻辑（如被召唤后移动到指定位置、执行特定路线）。"																
		Leaf::Action::Drive::DriveToFollowPosition	跟随位置驾驶：载具驾驶到指定跟随位置（如 “跟随队友后方 10 米处行驶”）。	用于控制实体（如车辆、机器人）驶向并保持跟随目标位置的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “驶向并跟随位置”，用于定义或关联实体执行跟随驾驶逻辑的核心指令（如保持与目标的相对位置、动态调整行驶轨迹）。"																
		Leaf::Action::Drive::DriveToFollowSlot	跟随插槽驾驶：载具驾驶到预设的 “跟随插槽”（如车队中的指定位置、护航队列位置）。	这是一个用于  控制实体（如车辆、机器人）驶向并保持跟随目标 “槽位”（Slot）的行为树节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “驶向并跟随槽位”，用于定义或关联实体执行跟随槽位逻辑的核心指令（如定位目标槽位、保持在槽位的相对位置）。"																
		Leaf::Action::Drive::DriveToFollowSpline	沿样条线跟随驾驶：载具沿预设样条线，跟随前方目标行驶（如车队沿固定路线护航）。	这是一个用于控制实体（如车辆、机器人）沿样条曲线跟随行驶的行为树节点，名为DriveToFollowSpline，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “沿样条曲线跟随行驶”，用于定义或关联该跟随行为的核心指令。
2. Use Kinematic（使用运动学）
类型：Bool（布尔型），当前为false
作用：选择是否采用运动学物理规则控制实体运动（适用于追求精准轨迹的跟随场景，如沿曲线的物理模拟）。
3. Need Driver（需要驾驶员）
类型：Bool（布尔型），当前为false
作用：设置实体是否需要 “驾驶员” 逻辑（如模拟有人驾驶的 AI 决策，或无人自动驾驶的差异）。
4. Spline（样条曲线）
类型：NodeRef（节点引用）
作用：关联跟随的样条曲线，实体将沿该曲线执行跟随行驶。
5. Secure Time Out（安全超时）
类型：Float（浮点型），当前为0
作用：设置跟随过程中的安全超时阈值，避免实体因异常卡顿而无限等待。
6. Backwards / Reverse（向后 / 反向）
类型：Bool（布尔型），当前均为false
作用：控制实体是否向后行驶或沿曲线反向跟随。
7. Closest（最近点开始）
类型：Bool（布尔型），当前为false
作用：启用后实体从曲线上距离自身最近的点开始跟随，而非从曲线起点。
8. Forced Start Speed（强制启动速度）
类型：Float（浮点型），当前为0
作用：设置实体跟随的初始速度，用于模拟 “起步加速” 等场景。
9. Stop At Path End（路径终点停止）
类型：Bool（布尔型），当前为false
作用：启用后实体到达曲线终点时停止跟随。
10. 距离与橡胶带（Keep Distance / Rubber Banding）系列参数
作用：用于控制实体与其他跟随单位的距离（如保持跟车距离），或通过 “橡胶带” 逻辑模拟跟随中的距离约束（如防止落后过多时瞬间追上）。"																
		Leaf::Action::Drive::DriveStunned	眩晕驾驶：载具处于眩晕状态下的驾驶逻辑（如失控摇晃、减速停车）。	控制实体（如车辆、机器人）进入 “眩晕 / 停滞” 驾驶状态的行为树节点，名为DriveStunned，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “眩晕驾驶控制”，用于定义或关联实体进入眩晕状态的驾驶指令（如因受击、异常状态导致的行驶停滞、无响应）。"																
		Leaf::Action::Drive::DriveToFollowTarget	跟随目标驾驶：载具持续跟随指定目标移动（如 “追击逃跑的敌人载具”）。	这是一个用于控制实体（如车辆、机器人）驶向并跟随目标对象的行为树节点，名为DriveToFollowTarget，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “驶向并跟随目标”，用于定义或关联实体执行跟随目标逻辑的核心指令。
2. Use Kinematic（使用运动学）
类型：Bool（布尔型），当前为false
作用：选择是否采用运动学物理规则控制实体运动（适用于追求精准轨迹的跟随场景，如模拟真实物理的跟随行为）。
3. Need Driver（需要驾驶员）
类型：Bool（布尔型），当前为false
作用：设置实体是否需要 “驾驶员” 逻辑（如模拟有人驾驶的 AI 决策，或无人自动驾驶的差异）。
4. Target（目标对象）
类型：Object（对象类型）
当前值：NULL
作用：指定实体需要跟随的目标对象（如玩家、NPC、其他载具等）。
5. Secure Time Out（安全超时）
类型：Float（浮点型），当前为0
作用：设置跟随过程中的安全超时阈值，避免实体因异常卡顿而无限等待。
6. Distance Min/Max（最小 / 最大距离）
类型：Float（浮点型），当前均为0
作用：定义实体与目标对象之间的距离约束范围，实体将保持在该距离区间内跟随目标。
7. Is Player（是否为玩家）
类型：Bool（布尔型），当前为false
作用：标识目标对象是否为玩家，用于触发针对玩家的特殊跟随逻辑（如优先级、交互规则）。
8. Stop Has Reached Target（到达目标时停止）
类型：Bool（布尔型），当前为false
作用：启用后实体到达目标对象时停止跟随。
9. Use Traffic（使用交通规则）
类型：Bool（布尔型），当前为false
作用：选择是否让实体在跟随过程中遵循交通规则（如避让、车道行驶）。
10. Allow Stub Movement（允许短距移动）
类型：Bool（布尔型），当前为false
作用：设置是否允许实体执行短距离的微调移动，确保跟随行为的平滑性。"																
		Leaf::Action::Drive::DriveToNodeRel	相对节点驾驶：载具驾驶到相对于某个节点的指定位置（如 “停在任务节点前方 5 米处”）。	控制实体（如车辆、机器人）驶向并到达指定节点引用（Node Ref）位置的行为树节点		" Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “驶向节点引用”，用于定义或关联实体驶向目标节点的核心指令。
2. Use Kinematic（使用运动学）
类型：Bool（布尔型），当前为false
作用：选择是否采用运动学物理规则控制实体运动（适用于追求精准轨迹的行驶场景）。
3. Need Driver（需要驾驶员）
类型：Bool（布尔型），当前为false
作用：设置实体是否需要 “驾驶员” 逻辑（如模拟有人驾驶的 AI 决策，或无人自动驾驶的差异）。
4. Node Ref（节点引用）
作用：关联场景中预设的目标节点，实体将驶向该节点对应的位置。
5. Stop At Path End（路径终点停止）
类型：Bool（布尔型），当前为false
作用：启用后实体到达节点路径终点时停止行驶。
6. Secure Time Out（安全超时）
类型：Float（浮点型），当前为0
作用：设置行驶过程中的安全超时阈值，避免实体因异常卡顿而无限等待。
7. Is Player（是否为玩家）
类型：Bool（布尔型），当前为false
作用：标识目标节点是否与玩家强关联，用于触发针对玩家的特殊行驶逻辑（如优先级、交互规则）。
8. Use Traffic（使用交通规则）
类型：Bool（布尔型），当前为false
作用：选择是否让实体在行驶过程中遵循交通规则（如避让、车道行驶）。
9. Speed In Traffic（交通中速度）
类型：Float（浮点型），当前为0
作用：设置实体在遵循交通规则时的行驶速度。
10. Force Green Lights（强制绿灯）
类型：Bool（布尔型），当前为false
作用：启用后实体可强制忽略交通灯约束，直接通行。
11. Portals（传送门）
当前值：NULL
作用：关联场景中的传送门节点，实体可通过传送门实现跨区域行驶。
12. Traffic Try Neighbors For Start/End（交通中尝试邻居节点起始 / 结束）
类型：Bool（布尔型），当前均为false
作用：设置实体在交通场景中是否尝试以邻居节点作为行驶的起始或结束点，增强路径规划的灵活性。
节点功能与应用场景"																
		Leaf::Action::Drive::DriveToPoint	驾驶到点：载具驾驶到指定世界坐标点（如 “前往地图标记的任务地点”）。	这是一个用于控制实体（如车辆、机器人）驶向指定坐标点的行为树节点，名为DriveToPoint，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “驶向坐标点”，用于定义或关联实体驶向目标点的核心指令。
2. Target Position（目标位置）
类型：Vector（三维向量），当前为{0, 0, 0}
作用：指定实体需要驶向的全局坐标位置（以场景全局坐标系为参考），例如设置为{10, 5, 0}，实体将向该场景坐标点行驶。
3. Secure Time Out（安全超时）
类型：Float（浮点型），当前为0
作用：设置行驶过程中的安全超时阈值，避免实体因异常卡顿而无限等待。
4. Use Traffic（使用交通规则）
类型：Bool（布尔型），当前为false
作用：选择是否让实体在行驶过程中遵循交通规则（如避让、车道行驶）。
5. Speed In Traffic（交通中速度）
类型：Float（浮点型），当前为0
作用：设置实体在遵循交通规则时的行驶速度。
6. Force Green Lights（强制绿灯）
类型：Bool（布尔型），当前为false
作用：启用后实体可强制忽略交通灯约束，直接通行。
7. Portals（传送门）
当前值：NULL
作用：关联场景中的传送门节点，实体可通过传送门实现跨区域行驶。
8. Traffic Try Neighbors For Start/End（交通中尝试邻居节点起始 / 结束）
类型：Bool（布尔型），当前均为false
作用：设置实体在交通场景中是否尝试以邻居节点作为行驶的起始或结束点，增强路径规划的灵活性。"																
		Leaf::Action::Drive::DriveToSplineReverse	反向样条线驾驶：载具沿预设样条线反向行驶（如 “沿巡逻路线返程”）。	控制实体（如车辆、机器人）沿样条曲线反向行驶的行为树节点，名为DriveToSplineReverse		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “沿样条曲线反向行驶”，用于定义或关联该反向行驶行为的核心指令。
2. Spline（样条曲线）
类型：NodeRef（节点引用）
作用：关联目标样条曲线，实体将沿该曲线执行反向行驶（即从曲线终点向起点方向行驶）。"																
																			
		Leaf::Action::ItemHandling::EquipSpecificltemToSlot	装备指定物品到插槽：将指定物品装备到 AI 的对应插槽（如 “把剑装备到主手插槽”“把护盾装备到副手插槽”）。	控制实体将特定物品装备到指定槽位的行为树节点，名为EquipSpecificItemToSlot		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “装备特定物品到槽位”，用于定义或关联该装备行为的核心指令。
2. Slot Id（槽位 ID）
当前值：None
作用：指定需要装备物品的目标槽位 ID（如武器槽、防具槽、道具槽等），明确装备的位置。
3. Item Id（物品 ID）
当前值：None
作用：指定需要装备的特定物品 ID，确保实体装备的是目标物品。
4. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置装备动作的持续时长，使装备过程表现得更真实（如角色抬手装备武器的动画时长）。
5. Fail If Item Not Found（物品未找到则失败）
类型：Bool（布尔型），当前为false
作用：设置当指定物品不存在时，该行为是否判定为失败。true时物品不存在则行为失败，false时可忽略该情况或执行备用逻辑。
6. Spawn Delay（生成延迟）
类型：Float（浮点型），当前为0
作用：设置物品生成到槽位的延迟时间，用于模拟 “物品生成后再装备” 的逻辑。"																
		Leaf::Action::ItemHandling::UnequipltemFromSlot	从插槽卸下物品：将 AI 对应插槽中的物品卸下（如 “卸下主手武器”“收起盾牌”）。	控制实体将指定槽位的物品卸下的行为树节点，名为UnequipItemFromSlot，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “从槽位卸下物品”，用于定义或关联该卸下行为的核心指令。
2. Slot Id（槽位 ID）
当前值：None
作用：指定需要卸下物品的目标槽位 ID（如武器槽、防具槽、道具槽等），明确卸下物品的位置。
3. Duration（持续时间）
类型：Float（浮点型），当前为0
作用：设置卸下动作的持续时长，使卸下过程表现得更真实（如角色放下武器的动画时长）。"																
		Leaf::Action::MountHandling::MountObject	挂载对象：AI 挂载指定对象（如 “骑上坐骑”“装备重型武器”）。	用于控制实体 “挂载 / 搭载” 目标对象的行为树节点，名为MountObject，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “挂载对象”，用于定义或关联实体执行挂载行为的核心指令（如角色登上载具、机器人搭载设备）。
2. Mount Data（挂载数据）
当前值：NULL
作用：存储或关联与挂载行为相关的数据（如挂载目标的信息、挂载位置、挂载后的状态参数等），用于控制挂载的具体逻辑。"																
		Leaf::Action::MountHandling::Unmount	卸载对象：AI 卸载当前挂载的对象（如 “下马”“收起重型武器”）。	控制实体 “卸载 / 脱离” 已挂载目标对象的行为树节点，名为Unmount，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “卸载对象”，用于定义或关联实体执行脱离挂载行为的核心指令（如角色从载具上跳下、机器人与搭载设备分离）。
2. Mount Data（挂载数据）
当前值：NULL
作用：存储或关联与卸载行为相关的数据（如挂载目标的信息、卸载位置、卸载后的状态参数等），用于控制卸载的具体逻辑。"																
		Leaf::Action::Move Along Traffic Path	沿交通路径移动：AI 沿预设的交通路径移动（如城市道路的人行道、车道，模拟真实行人 / 车辆移动）。	用于控制实体沿交通路径移动的行为树节点，名为Move Along Traffic Path，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “沿交通路径移动”，用于定义或关联实体在交通系统中沿路径行驶的核心指令。
2. Join Traffic Settings（加入交通设置）
当前值：@None
作用：存储或关联实体加入交通流的相关设置（如交通规则、优先级等），确保实体融入交通系统的逻辑合规。
3. Ignore Traffic Spots（忽略交通点）
类型：Bool（布尔型），当前为false
作用：设置实体是否忽略交通系统中的节点或关键点，用于特殊场景下的路径规划（如紧急通行）。
4. Use Crowd Animation Graph（使用人群动画图）
类型：Bool（布尔型），当前为false
作用：选择是否为实体应用人群动画逻辑（如模拟行人在交通路径中的自然移动动画）。
5. Workspot 系列参数（Workspot Data、Exit Position 等）
作用：用于定义实体在交通路径中的 “工作点” 相关数据，如工作点位置、退出位置、返回位置等，支持实体在交通路径中执行临时作业或状态切换。
6. Traffic Lane 系列参数（Traffic Lane Return Tangent、Exit Tangent 等）
作用：控制实体在交通车道中的切线方向（如车道进入、退出时的方向控制），确保实体沿车道的移动方向符合交通规则。"																
		Leaf::Action::Patrol	巡逻动作：AI 按预设巡逻路线移动（如 “绕基地巡逻”“沿街道往返巡逻”），包含路径点切换、警戒逻辑。	这是一个用于控制实体执行巡逻行为的行为树节点，名为Patrol，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “巡逻”，用于定义或关联实体执行巡逻逻辑的核心指令。
2. Path（巡逻路径）
当前值：@None
作用：指定实体执行巡逻的路径（如预设的巡逻路线、区域边界等），实体将沿该路径循环或往复移动。
3. Patrol Progress（巡逻进度）
当前值：@None
作用：存储或关联巡逻行为的进度数据（如当前巡逻到路径的哪个节点、剩余巡逻段等），用于控制巡逻的连续性和状态。
4. Start From Closest Point（从最近点开始）
类型：Bool（布尔型），当前为false
作用：启用后实体从巡逻路径上距离自身最近的点开始巡逻，而非从路径起点。
5. Play Start Animation（播放起始动画）
类型：Bool（布尔型），当前为false
作用：设置实体开始巡逻时是否播放起始动画（如角色 “准备巡逻” 的动作动画）。
6. Workspot Data（工作点数据）
当前值：@None
作用：关联巡逻过程中实体的 “工作点” 数据（如巡逻途中的检查点、交互点），支持实体在巡逻时执行临时作业。
7. Dependent Workspot Data（依赖工作点数据）
当前值：@None
作用：关联与巡逻工作点相关的依赖数据，用于控制工作点的交互逻辑或状态依赖。"																
		Leaf::Action::RotateToObjectConstTime	固定时间转向对象：AI 在指定时间内（如 2 秒）平稳转向目标对象（避免瞬间旋转，更自然）。	这是一个用于控制实体在指定时间内旋转朝向目标对象的行为树节点，名为RotateToObjectConstTime		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “恒定时间旋转朝向对象”，用于定义或关联实体执行该旋转行为的核心指令。
2. Target（目标对象）
类型：Object（对象类型）
当前值：NULL
作用：指定实体需要朝向的目标对象（如玩家、NPC、特定物体等）。
3. Angle Offset（角度偏移）
类型：Float（浮点型），当前为0
作用：设置实体朝向目标时的角度偏移量，用于调整朝向的精准度（如让实体面朝目标的左 / 右方一定角度）。
4. Angle Tolerance（角度容差）
类型：Float（浮点型），当前为0
作用：定义实体朝向目标时的角度误差容忍范围，当实体与目标的角度差在该范围内时，判定为朝向成功。
5. Time（旋转时间）
类型：Float（浮点型），当前为0
作用：设置实体完成朝向目标旋转的总时间，控制旋转的速度和节奏。
6. Keep Updating Target（持续更新目标）
类型：Bool（布尔型），当前为false
作用：启用后实体在旋转过程中持续更新目标对象的位置，确保即使目标移动，也能准确朝向。"																
		Leaf::Action::Scene Animation Motion	场景动画动作：AI 执行与场景绑定的动画动作（如 “触发剧情时的特定动作”“与场景道具交互的动画”）。	这是一个用于控制实体执行场景动画运动的行为树节点，名为Scene Animation Motion，属于动作节点		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “场景动画运动”，用于定义或关联实体执行场景动画驱动的运动行为的核心指令。
2. Params（参数）
作用：存储或关联场景动画运动的相关参数（如动画名称、运动速度、循环次数等），用于控制动画运动的具体表现。
3. Mount Data（挂载数据）
当前值：None
作用：若实体在动画运动过程中涉及挂载 / 搭载逻辑，可通过该参数关联挂载数据，确保运动与挂载状态的协同。"																
		Leaf::Action::UseCommunityWorkspot	使用公共工作点：AI 使用公共工作点资源（如 “使用公共饮水机”“在共享工作台制作物品”），支持多 AI 排队使用。	这是一个用于 ** 控制实体使用社区工作点（Workspot）** 的行为树节点，名为UseCommunityWorkspot，属于动作节点。		"1. Command（命令）
类型：Serializable（可序列化），参数化类型为CustomValue（自定义值）
当前值：NULL
作用：标识节点的核心功能为 “使用社区工作点”，用于定义或关联实体与社区工作点交互的核心指令。
2. Workspot Data（工作点数据）
当前值：NULL
作用：关联社区工作点的具体数据（如工作点位置、交互规则、可用功能等），明确实体要使用的工作点。
3. Dependent Workspot Data（依赖工作点数据）
当前值：NULL
作用：关联与该社区工作点相关的依赖数据，用于控制工作点交互的前置条件或状态依赖。
4. Play Exit Automatically（自动播放退出）
类型：Bool（布尔型），当前为false
作用：设置实体使用完工作点后是否自动执行退出动画或逻辑，简化交互流程。
5. Fast Forward After Teleport（传送后快进）
类型：Bool（布尔型），当前为false
作用：若实体通过传送到达工作点，启用后可快进后续的交互或动画流程，提升效率。"																
		Leaf::Failer	失败节点：执行后直接返回 “失败” 状态（用于强制触发分支逻辑，如 “模拟某个行为失败，触发备用方案”）。	用于返回行为树状态		1.Debug log Command 																
		Leaf::Idle	 idle 节点：AI 执行空闲行为（如 “站立不动”“随机环顾四周”“播放 idle 动画”），返回 “成功” 状态。	用于返回行为树状态		1.Debug log Command 																
		Leaf::Succeeder	成功节点：执行后直接返回 “成功” 状态（用于强制触发分支逻辑，如 “模拟某个行为成功，跳过备用方案”）。	用于返回行为树状态		1.Debug log Command 																
	Netsed Tree																					
		Netsed Tree::IncludedTree																				
		Netsed Tree::SubTree																				
	Stat																					
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
																						
